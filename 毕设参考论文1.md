# Transparent SNARKs over Galois Rings

Yuanju $\mathrm {Wei}^{1,2[0009-0008-6778-9794]}$ ,Xinxuan Zhang1,2[0000-0002-2739-7656],and

Yi Deng1,2[0000-0001-5948-0780]

State Key Laboratory of Information Security, Institute of Information

Engineering, Chinese Academy of Sciences, Beijing,China

2 School of Cyber Security, University of Chinese Academy of Sciences,Beijing,China

{weiyuanju,zhangxinxuan, deng}@iie.ac.cn

**Abstract.** Recently, there is a growing need for SNARKs to operate over a broader range of algebraic structures, and one important structure is Galois ring. We present transparent SNARK schemes over arbitrary Galois rings. Compared with Rinocchio scheme in Ganesh et al. (J Cryp-tol 2023), our SNARK schemes do not require a trusted third party to establish a structured reference string (SRS).

In this paper, we present the expander code over arbitrary Galois rings, which can be encoded in $O()$ time. Using this expander code, we then extend the Brakedown commitment scheme in Golovnev et al. (CRYPTO 2023) to Galois rings. By combining the Libra framework in Xie et al. (CRYPTO 2019), we present a transparent SNARK for log-space uniform circuits over Galois rings, achieving $O(n)$  prover time, $O(\sqrt {n})$ proof size,and $O(\sqrt {n})$ verifier time. And by combining Hyper-Plonk in Chen et al. (EUROCRYPT 2023), we present a transparent SNARK for NP circuits over Galois rings, with $O\left(n\log ^{2}n\right)$ prover time, $O(\sqrt {n})$  proofsize,and $O(\sqrt {n})$  verifier time.

**Keywords**: SNARKs·Galois rings·polynomial commitment.

## 1 Introduction

Succinct Non-interactive Arguments of Knowledge (SNARK) are cryptographic protocols that allow a verifier toefficiently check the validity of any NP state-ment without interacting with the prover [25,27,20,8]. One of the most important security properties of SNARKs is soundness. Soundness demands that for any incorrect NP statement, it is infeasible for any prover to generate a proof that will pass verification. To ensure the soundness property, SNARK protocols are typically designed for arithmetic circuits over a large prime field $\mathbb {F}_{p}$ .Recently, there is a growing demand to deploy them over more general algebraic struc-tures. While arithmetic circuits over large prime fields $\mathbb {F}_{p}$ can simulate various algebraic structures, such simulation often comes at an expensive efficiency cost. It is more practical to design SNARKs directly for arithmetic circuits tailored to specific algebraic structures. Arithmetic circuits over rings are both a natural extension of field-based arithmetic circuits and hold significant value in vari-ous applications. One important use case is in Fully Homomorphic Encryption (FHE), where computations are performed over rings, and the need for SNARKs to prove correctness in these settings has gained attention. In particular, second-generation FHE schemes like BGV and (B)FV[11,12] are defined over large in-teger rings. In this paper, we focus on Galois rings. Galois rings are a type of finite commutative rings which generalize both the finite fields and the rings of integers modulo a prime power. According to the Chinese Remainder Theorem, any integer ring (extension) can be mapped one-to-one to several Galois rings.

<!-- 2 -->

<!-- Yuanju Wei,Xinxuan Zhang, and Yi Deng -->

SNARKs for arithmetic circuits over rings have garnered significant atten-tion, but there are relatively few solutions specifically tailored for such settings. The Rinocchio protocol, proposed by Ganesh et al. [19], is the first complete SNARK designed for ring-based arithmetic circuits. Rinocchio is a ring version of the Pinocchio [28] and the Groth16 [23]. These SNARKs are based on Linear PCP constructions, and they require a trusted setup to generate a structured reference string (SRS). In recent years, newer SNARK schemes have moved away from Linear PCP-based constructions, instead adopting a combination of PIOP and polynomial commitment schemes. Examples include Libra [33], Plonk [18], Spartan [29], Marlin [16], STARK [4,3,5], Brakedown [22], and Orion [34].Many of these do not require a trusted third party to set up an SRS. However, none of these can be directly applied to arithmetic circuits over rings because, to date, there are no known polynomial commitment schemes forring arithmetic.

Current polynomial commitment schemes over finite fields can generally be classified into three categories. The first category is the KZG commitment scheme based on pairing structures, proposed by Kate et al. [24]. And it also relies on an SRS. The second category is based on the hardness of the discrete logarithm problem, such as Bulletproofs proposed by Bünz et al. [13] and Dory proposed by Lee [26]. The third category is based on encoding techniques, such as FRI (Fast Reed-Solomon Interactive Oracle Proof of Proximity) proposed by Ben-Sasson et al. [3,5], Brakedown proposed by Golovnev et al. [22], and Basefold proposed by Zeilberger et al. [35]. These polynomial commitment schemes rely on algebraic structures applicable to finite fields, such as pairing structures, which1 are difficult to satisfy over Galois rings. Thus, this naturally leads to the following question:

Is it possible to construct polynomial commitments over Galois rings, and furthermore, is there a transparent SNARK scheme over Galois rings?

We found that the Brakedown scheme only relies on linear codes with fixed relative distance. Since linear codes are typically designed for finite fields,and given the “similarity” between Galois rings and finite fields, the Brakedown scheme appears to be the most feasible scheme to implement over Galois rings, and it does not require a trusted setup for generating an SRS. Therefore, we present the Brakedown commitment scheme over Galois rings and proposes a SNARK scheme over arbitrary Galois rings without the need for an SRS.

### 1.1 Our Contribution

We present a polynomial commitment and transparent SNARK schemes over arbitrary Galois rings in this paper.

<!-- Transparent SNARKs over Galois RRings -->

<!-- 3 -->

We extend expander codes to arbitrary Galois rings, enabling linear-time encoding with a fixed relative distance that is a constant depending on the Galois ring. Then, using this encoding, we present the Brakedown polynomial commitment scheme over Galois rings. Combining this construction with Libra, we present a transparent SNARK for log-space uniform circuits over Galois rings, achieving $O(n)$  prover time, $O(\sqrt {n})$ proof size,and $O(\sqrt {n})$ verifier time.And by combining HyperPlonk, we present a transparent SNARK for NP circuits over Galois rings with $O\left(n\log ^{2}n\right)$ prover time, $O(\sqrt {n})$ proof size,and $O(\sqrt {n})$ verifier time.

### 1.2 Technique Overview

We revisit the Brakedown polynomial commitment. A multilinear polynomial $f$ with l variables can be written as

$$f\left(x_{1},\cdots ,x_{l}\right)=\sum _{\mathbf {b}\in \{0,1\}^{l}}\prod _{i\in [1,l]}\left(\left(1-x_{i}\right)\left(1-b_{i}\right)+x_{i}b_{i}\right)f(\mathbf {b})$$

The values of $f$  over the hypercube can be viewed as the basis of the poly-nomial,resulting in $2^{l}$ basis elemnents. These $2^{l}$  elements can be arranged into a $2^{l/2}\times 2^{l/2}$ matrix $S$ :

$$\begin{bmatrix}f(0,\cdots ,0,0,\cdots ,0)\;f(0,\cdots ,0,0,\cdots ,1)\cdots f(0,\cdots ,0,1,\cdots ,1)\\ f(0,\cdots ,1,0,\cdots ,0)\;f(0,\cdots ,1,0,\cdots ,1)\cdots f(0,\cdots ,1,1,\cdots ,1)\\ :&:&:\\ f(1,\cdots ,1,0,\cdots ,0)\;f(1,\cdots ,1,0,\cdots ,1)\cdots f(1,\cdots ,1,1,\cdots ,1)\end{bmatrix}$$

To evaluate $f$  at a point $r$ , one computes

$$f\left(r_{1},\cdots ,r_{l}\right)=\sum _{\mathbf {b}\in \{0,1\}^{l}}\prod _{i\in [1,l]}\left(\left(1-r_{i}\right)\left(1-b_{i}\right)+r_{i}b_{i}\right)f(\mathbf {b})$$

Using matrix $S$ , this calculation involves two vectors, $\mathbf {S}_{1}$  and $\mathbf {s}_{2}$ ,each of length $2^{l/2}$ .Specifically,

$$\mathbf {s}_{1}=\left(\left(1-r_{1},r_{1}\right)\otimes \cdots \otimes \left(1-r_{l/2},r_{l/2}\right)\right)$$

and

$$\mathbf {s}_{2}=\left(\left(1-r_{l/2+1},r_{l/2+1}\right)\otimes \cdots \otimes \left(1-r_{l},r_{l}\right)\right)$$

Therefore, $f(\mathbf {r})=\mathbf {s}_{1}^{\top }S\mathbf {s}_{2}$ 

The core of the Brakedown commitment is to ensure that the prover can correctly compute $\mathbf {}_{1}^{\top }S$ using encoding techniques and then send the result to the verifier, who completes the remaining computation. By employing encoding methods as described in Ligero (AHIV17)[1], proposed by Ames et al., the verifier can ensure that the prover correctly forms the linear combination of the encoded vectors.

<!-- 4 -->

<!-- Yuanju Wei, Xinxuan Zhang, and Yi Deng -->

**Linear** **Codes** **over** **Galois** **Rings.** During the encoding process of expander codes over large prime fields $\mathbb {F}_{q}$ , a crucial step is performing linear combinations on $k$  non-zero elements. The linear combinations are performed $b(k)$  times, where $b(k)$  is a function of $k$  and $b()=\max (+4,$ 1.28k). Let $z$  is the probability that a single linear combination is zero. According to the union bound, the probability that all $b(k)$  linear combinations of any $k$  non-zero elements are zero is bounded by:

Pr[b(k)all $\text {zeros}]\leq q^{k}z^{b(k)}$ 

To ensure codeword distance, this probability must be negligible. While this is easily achievable in large prime fields, however, in Galois rings $\mathrm {GR}\left(p^{s},r\right)$ ,zero divisors significantly increases this probability, braking the code distance.

A key observation to solve this problem is that in a Galois ring $\text {GR}\left(p^{s},r\right)$ all zero divisors reside in the ideal $(p)$ , meaning the proportion of zero divisors is $\frac {1}{p^{r}}$ .If $p^{r}$  is exponentially large relative to the security parameter $λ$ ,then the proportion of zero divisors becomes negligible. However, the total number of elements in the Galois ring is $p^{sr}$ ,and as $p^{r}$  increases,the total number of elements in the Galois ring grows accordingly. We need that any $k$  non-zero elements satisfy the distance condition. At this time, $z=\frac {1}{p^{r}}$ ,so we have

$$\text {Pr}[b(k)\text {allzeros}]\leq p^{ksr}\left(\frac {1}{p^{r}}\right)^{b(k)}=\frac {p^{ksr}}{p^{b(k)r}}$$

In this case,as long as $s\geq 2,$ the probability will not be negligible.

We solve this problem by performing a more refined parameter analysis. By analyzing the distribution of zero divisors in the Galois ring, we catego-rize the zero divisors into $S$  classes based on their membership in the ideals $\left(^{-1}\right),\cdots ,\left(^{2}\right),()$ .Each element is classified according to the smallest ideal it belongs to. For instance, if an element is in the ideal $\left(p^{i}\right)$ ,it is also contained in $\left(p^{i-1}\right)$ , but it is classified as being in the $i$ -th class rather than the $(i-1)$ th class. We then analyze the impact of the zero divisors from each of these $S$ classes on the code distance. As $i$  increases, the probability that a random linear combination of elements from the ideal $\left(p^{i}\right)$ results in zero increases, thus having a greater impact on the code's distance. However, the proportion of such zero divisors in the entire Galois ring decreases. Through this more detailed parame-ter analysis, we prove that expander codes can maintain a constant relative code distance with $1-$ negligible probability in Galois rings $\text {GR}\left(p^{s},r\right)$  , provided that $p^{r}$ is sufficientlylarge (exponential scale relative to the security parameter $λ$ ).

If the Galois ring $\text {GR}\left(p^{s},r\right)$  does not satisfy the large condition, we draw in-spiration from the block-level encoding approach introduced in [17] and adapt it from binary fields to arbitrary Galois rings. Specifically, if a Galois ring $\text {GR}\left(p^{s},r\right)$ requires an expansion by a factor of $k$  to meet the necessary conditions, we treat $k$  elements from GR $\left(p^{s},r\right)$ as a single element in GR $\left(p^{},kr\right)$ during the encoding

<!-- Transparent SNARKs over Galois Rings -->

<!-- 5 -->

process. We further demonstrate that this encoding method maintains linearity over $\text {GR}\left(p^{s},r\right)$ . This construction thus results in a linear code defined over any Galois ring GR $\left(p^{},r\right)$ 

**Polynomial Commitments over Galois Rings.** Using the linear codes de-fined over Galois rings, we apply the Brakedown construction framework to present a polynomnial commitment scheme over Galois rings. Since Brakedown commitments rely on the linear code detection lemma from AHIV17 [1], we must first discuss this lemma for arbitrary Galois rings. If a Galois ring GR( $\left(p^{},\right)$ does not satisfy the condition that $p^{r}$  is sufficiently large, we can say this ring is a “small ring”. We need to account for cases where the polynomial to be committed is defined over a small ring.

Reviewing the construction of the Brakedown polynomial commitment,the verifier must ensure that the prover correctly performs a linear combination on each row of the coefficient matrix. In the encoding step, we mentioned that adja-cent $k$ elements in the small ring are treated as a single element in the extension ring for encoding. If the verifier directly selects a challenge $e$  from the exten-sion ring, then $e$  will multiply with the larger element formed by the $k$  elements from the small ring. This results in the “mixing” of the original $k$  coefficients of the polynomial,meaning the operation is no longer within the small ring. In such scenarios, it is preferable for the verifier's challenge to be selected from $\text {GR}\left(p^{s},r\right)$ to ensure that the coefficients used in the linear combination of the codewords are drawn from $\text {GR}\left(p^{s},r\right)$ .However, if the verifier selects challenges only from the small ring, it cannot guarantee the soundness of the polynomial commitment.

We employed Interleaved codes and Block-wise relative distance [9] to discuss the "repetition” version of the lemma over Galois rings to solve this problem: the verifier can ensure soundness by repeatedly selecting challenges multiple times. This approach, to some extent, avoids the need for field expansion operations, leading to an improvement in computational efficiency.

In practical applications of polynomial commitment, it is often encountered that polynomial coefficients come from a smaller ring $\mathrm {GR}\left(p^{s},r\right)$  ,but for security reasons, the challenges are drawn from GR $\left(p^{}kr\right)$ ,such as sumcheck. In [17],this issue is solved by using a two-dimensional extension. In this work, we explain the problem from the perspective of “repetition”, using a more intuitive expression. When the prover computes $\mathbf {s}_{1}^{\top }S$ ,only $\mathbf {S}_{1}$  comes from GR $\left(p^{}kr\right)$ .Therefore, each element of $\mathbf {S}_{1}$  can be broken down into $k$ -dimensional vectors, with each dimension being computed separately. This allows for a more efficient polynomial commitment process by avoiding the need to pad each element in GR $\left(p^{},r\right)$ to the larger ring $\mathrm {GR}\left(p^{s},kr\right)$  during the commitment phase.

**SNARKs** **over** **Galois** **Rings.** We combine polynomial commitments over Ga-lois rings with PIOP (Polynomial Interactive Oracle Proof) to obtain SNARKs schemes over Galois rings. While sumcheck protocols over rings have been widely discussed, such as the extension to infinite non-commutative rings in [30],some PIOP frameworks those involving set consistency checks, cannot be directly ex-tended from fields to Galois rings. This issue arises from the presence of zero divisors in Galois rings, which complicates set consistency checks.

<!-- 6 -->

<!-- Yuanju Wei, Xinxuan Zhang, and Yi Deng -->

In a field, to determine whether two sets $S_{1}$ and $S_{2}$ are identical,we encode them as polynomials, $p_{1}(x)=\prod _{\in S_{1}}(x-)$ and $p_{2}(x)=\prod _{a\in S_{2}}(x-a)$ ,and compare the polynomials. However, in Galois rings, zero divisors can result in different sets being encoded as identical polynomials. For example, under modulo $8$ ,we have $(x-1)(x-7)=(x-5)(x-3)$  (mod 8).

So we selected an IOP framework that avoids set consistency checks: Libra and HyperPlonk. Using Libra, we constructed a transparent SNARK scheme for log-space uniform circuits over Galois rings, with $O(n)$ prover time, $O(\sqrt {n})$ proof size,and $O(\sqrt {n})$  verifier time. Similarly, using HyperPlonk, we constructed a transparent SNARK for NP circuits over Galois rings,achieving $O\left(n\log ^{2}n\right)$ prover time, $O(\sqrt {n})$ proof size,and $O(\sqrt {n})$ verifier time.

### 1.3 Related Work

**Proof** **Systems** **over** **Rings.** Due to the widespread application of proof sys-tems, some efforts have been made to extend these systems to broader algebraic structures. Several works aim to migrate proof systems to arithmetic circuits over rings. In [15], Chen et al. presented the sumcheck and GKR protocols to fi-nite commutative rings, where the verifier's challenges aare required to come from an exceptional set within the ring. In [10], Bootle et al. constructed a sumcheck protocol over rings and used it to solve the Rank-1 Constraint System (R1CS) problem over rings. Furthermore, Soria-Vazquez extended the sumcheck proto-col to infinite non-commutative rings in [30]. These protocols are rooted in the information-theoretic framework. However, due to the absence of a polynomial commitment scheme, none of these protocols are complete SNARK schemes.

Ganesh et al. [19] proposed the Rinocchio protocol, the first SNARK scheme for arithmetic circuits over rings.This protocol is based on the Linear PCP framework and does not require a polynomial commitment, but it does rely on a trusted third party to generate a structured reference string (SRS), which can raise concerns in practical apoplications. In recent years, new SNARK schemes have been introduced that eliminate the need for an SRS but instead depend on polynomial commitments. However, there is no polynomial commitment schemes over rings. In this paper, we present the Brakedown polynomial commitment scheme over Galois rings and present a SNARK based on an $\text {PIOP+polynomia}$ commitment framework, differing from Rinocchio. Unlike Rinocchio,the new SNARKs do not require an SRS.

**Code based Polynomial Commitment.** Some of the latest polynomial com-mitment schemes are code-based. Compared to previous approaches, these com-mitment schemes only rely on collision-resistant hash functions cryptographic primitive, and they are transparent.

The first representative scheme in this category is the FRI (Fast Reed-Solomon Interactive Oracle Proof of Proximity) scheme, proposed by Ben-Sasson et al. [3,5]. FRI-based polynomial commitments exploit the efficient detection of Reed-Solomon codes to commit to polynomials. However, this scheme requires an“FFT-friendly” field and uses an iterative structure similar to FFT,resulting in a prover time complexity of $O(n\log n)$  .

<!-- Transparent SNARKs over Galois Rings -->

<!-- 7 -->

Another key scheme based on encoding is the Brakedown scheme,proposed by Golovnev et al. [22]. The construction of Brakedown is based on linear codes with fixed relative distances and the only cryptographic primitive used is a collision-resistant hash function. Brakedown provides linear-time prover efficiency but with larger proof sizes and slower verifier time.

Additionally,the recently proposed Basefold protocol by Zeilberger et al. [35] is also based on encoding techniques and serves as a trade-off between FRI and Brakedown. Although Basefold retains the same asymptotic complexity of $O(n\log n)$ ,it offers faster prover times compared to FRI and an improved verifier time of $O(\log n)$ , whereas Brakedown has a verifier time of $O(\sqrt {}$ .The Basefold construction depends on foldable linear codes.

Given these comparisons, Brakedown has the minimum coding requirement and is the most “friendly” for Galois rings, whichis why we choose the Brakedown framework.

## 2 Preliminaries

We denote a finite field by $\mathbb {F}$ , a security parameter by $λ$ , and a negligible function with respect to $λ$  by $negl(λ)$ . We use PPT to denote probabilistic polynomial time. For any integer $n$ , we define $\text {Poly}(\mathbb {F},n)$  as the set of polynomials with n variables and coefficients in the field $\mathbb {F}$ .

### 2.1 Galois Rings

In this part, we will focus on discussing certain properties of Galois rings that will be used later. These properties are fundamental to establishing codes over Galois rings.

A Galois ring is constructed from the ring $\mathbb {Z}/p^{}\mathbb {Z}$ similar to how a finite field $\mathbb {F}_{p^{}}$ is constructed from $\mathbb {F}_{p}$ [32]. It is a Glaois extension of $\mathbb {Z}/p^{s}\mathbb {Z}$ when the concept of Galois extension is generalized beyond the context of fields.

**Definition** **1.** A Galois ring is a commutative ring of characteristic $p^{s}$  which has $p^{r}$  elements,where $p$  is a prime and s and $r$  are positive integers. It is usually denoted( $GR\left(p^{s}\right.$ $,r)$ .It can be defined as a quotient ring

$$GR\left(p^{s},r\right)\cong \mathbb {Z}[x]/\left(p^{s},f(x)\right)$$

where $f(x)\in \mathbb {Z}[x]$  is monic polynomial of degree $r$  which is irreducible modulo p. $Up$ to isomorphism, the ring depends only on p, $n$ ,and $r$  and not oon the choice of f used in the construction.

Every Galois ring is a local ring. The unique maximal ideal is the principal ideal $(p)=p\mathrm {GR}\left(p^{s},r\right)$ , consisting of all elements which are multiples of p. Further-more, $(0)\left(p^{-1}\right)\cdots (p)(1)$ are all the ideals.

<!-- 8 -->

<!-- Yuanju Wei,Xinxuan Zhang, and Yi Deng -->

If a Galois ring $\text {GR}\left(p^{s},r\right)$ does not satisfy the condition that $p^{r}$ is exponential scale relative to the security parameter $\lambda$ , we can say this ring is a “small ring”.

**Fact** **1** All zero divisors in the Galois ring $GR\left(p^{s},r\right)$  are in the ideal $(p)$ 

We define a ring homomorphism $Φ$ 

<!-- $\begin{array}{c}\mathbb {Z}_{p^{s}}\\ c_{0}+c_{1}p+\cdots +c_{p-1}p^{s-1}\mapsto c_{0}\end{array}$ -->

where $0\leq c_{i}\leq p-1$ and its kernel is the ideal $(p)$  of the ring $\mathbb {Z}_{p^{s}}$ .And the ring homomorphism can be extended to $ψ$ 

$$\begin{array}{ccc}\mathbb {Z}_{p^{s}}[x]&\rightarrow &\mathbb {F}_{p}[x]\\ a_{0}+a_{1}x+\cdots +a_{n}x^{n}&\mapsto \bar {a}_{0}+\bar {a}_{1}x+\cdots +\bar {a}_{n}x^{n}\end{array}$$

where $\bar {a}_{i}=φ\left(a_{i}\right)$  and the kernel of $ψ$  is the ideal $(p)$  of the ring $\mathbb {Z}_{p^{s}}[x]$ .So the ideal $(h(x))$ is the ideal $(\bar {h}(x$ . So we induce a ring homomorphism $Φ$ 

$$\begin{array}{ccc}\mathbb {Z}_{p^{s}}[x]/(h(x))&\rightarrow &\mathbb {F}_{p}[x]/(\bar {h}(x))\\ a_{0}+a_{1}x+\cdots +a_{r-1}x^{r-1}+(h(x))&\mapsto \bar {a}_{0}+\bar {a}_{1}x+\cdots +\bar {a}_{r-1}x^{r-1}+(\bar {h}(x))\end{array}$$

The kernel of the ring homomorphism $Φ$  is the ideal $(p+(h(x)))$ generated by $p+(h(x))$ in $\left(\mathbb {Z}_{p^{s}}[x]/h(x)\right)$ . By the fundamental theorem of homomoprphisms of rings

$$\left(\mathbb {Z}_{p^{s}}[x]/(h(x))\right)/(p+h(x))\;eq\mathbb {F}_{p}/(\bar {h}(x))$$

From the above series of ring homomorphisms, we observe that the Galois ring is an algebraic structure closely related to a field. Consequently, many techniques applicable to fields can be smoothly transferred to rings.

**Definition** **2.** a is an element of ring $GR\left(p^{},r\right)$  and $n$  is an integer. We define $\text {gcd}(a,n)$  as $\text {gcd}\left(a_{0},\cdots ,a_{r-1},n\right)$ .Where a is represented by $a_{0}+a_{1}x+\cdots +$ $a_{r-1}x^{r-1}$ 

**Fact** **2** Consider elements a and $b$  in the Galois ring $GR\left(p^{s},r\right)$ $\text {Let}d=\text {gcd}\left(a,p^{s}\right)$ The linear equation $ax=b$  has at most $d^{r}$  solutions within( $GR\left(p^{},r\right)$ 

Proof. If $\text {gcd}\left(a,p^{s}\right)=1$  then $a$  is not in the ideal $(p)$ . According to the properties of the Galois ring, $a$  is not a zero divisor in $\mathrm {GR}\left(p^{s},r\right)$ ,implying that $a$  has an inverse.Therefore, $x=b·^{-1}$ has a unique solution.

$\text {gcd}\left(a,p^{s}\right)=d$ then we have

$$ax=b\quad \left(bmod\left(p^{s},f\right)\right)$$

The condition for this equation to have a solution is $d|b_{i}$ for all $i,0\leq i\leq -1,$ where $b_{i}$  is the coefficient in $b$  Thus, we have

$\frac {a}{d}x=\frac {b}{d}$ $\left(bmod\left(p^{s}/d,f\right)\right)$ 

<!-- Transparent SNARKs over Galois Rings -->

<!-- 9 -->

where $\frac {a}{d}$  and $\frac {b}{d}$  mean each coefficient in $a$  and $b$  is divided by $d$ .Let $\frac {a}{d}=a^{\prime }$ and $\frac {b}{d}=b^{\prime }$ . Then $a^{\prime }$ ' and $b^{\prime }$ ' are elements of the Galois ring GR $\left(p^{s}/d,\right.$ r)..Since $\text {gcd}\left(a^{\prime },p^{s}/d\right)=1$ , $a'$  is not a zero divisor in the ring GR $\left(p^{}/d,\right)$ ,so $a^{\prime }$  has an inverse.Therefore, $x$  has a unique solution in the ring GR $\left(p^{}/dr\right)$ .This solution is obtained by determining each coefficient of $x$ .

$$x_{0}=c_{0}\quad (mod\quad \left.p^{s}/d\right)$$

$$x_{1}=c_{1}\quad \left(bmodp^{s}/d\right)$$

$x_{r-1}=c_{r-1}$  (mod $\left.p^{s}/d\right)$ 

So

$$x_{0}=c_{0}+r_{0}\frac {p^{s}}{d}\quad \left(bmodp^{s}\right),r_{0}\in [0,d-1]$$

$$x_{1}=c_{1}+r_{1}\frac {p^{s}}{d}\quad \left(bmodp^{s}\right),r_{1}\in [0,d-1]$$

$$x_{r-1}=c_{r-1}+r_{r-1}\frac {p^{s}}{d}\quad \left(bmodp^{s}\right),r_{d-1}\in [0,d-1]$$

Obviously $r_{i},i\in [0,d-1]$  has $d$  values, so each coefficient $x_{i}$ of $x$  has at most d values, and $x$  has at most $d^{r}$  values. ☐

**Fact** **3** In the Galois ring G $GR\left(p^{},r\right)$ ,the probability that a degree-d polynomial $f$  evaluates to zero at a randomly chosen point is at most $\frac {}{p^{r}}$ 

**Fact** **4** If a monic polynomial $f$  is irreducible in the field $GF(p,r)$ ,then $f$  is also irreducible in the Galois ring $GR\left(p^{},r\right)$ 

In Appendix A, we provide detailed proofs for 3 and 4.

**Definition 3 (Exceptional Set [19]).** Let $A=\left\{a_{1},\cdots ,a_{n}\right\}\subset R.$ .We say that $A$  is an exceptional set $\text {if}\forall i\neq j,$ $a_{i}-a_{j}\in R^{*}$ ,where $R^{*}$ is the set of all invertible elements in the ring R.

**Lemma 1 (Genralized Schwartz-Zippel Lemma [7,19]).Let** $f:$ $R^{n}\rightarrow R$ be an n-variate nonzero polynomial.Let $A\subseteq$ Rbe an finite exceptional set. Let $\text {deg}(f)$  denote the total degree of f. Then:

$$\text {Pr}_{\mathbf {a}\leftarrow A^{n}}[f(\mathbf {a})=0]\leq \frac {\text {deg}(f)}{|A|}$$

### 2.2 SNARKs

We adapt the definition from Rinocchio [19]. Let $R$  be an efficiently computable binary relation which consists of pairs of the form $(x,w)$  where $x$  is a statement

<!-- 10 -->

<!-- Yuanju Wei, Xinxuan Zhang, and Yi Deng -->

and $w$  is a witness. Let $L$  be the language associated with the relation $\mathcal {R}\text {,i.e.}\mathcal {L}=$ $\{x|\exists w,\text {s.t.}\mathcal {R}(x,w)=1\}$ 

A proof or argument system for R consists in a triple of PPT algorithms $\pi =(\text {Setup}$ **,Prove, Verify)** defined as follows:

$-\text {Setup}\left(1^{λ}\right)\rightarrow (σ,\mathrm {vk})$ ：take a security parameter $λ$  and outputs a common (sturctured) reference string $σ$  together with private verification information vk.

- $\text {Prove}(σ,x,w)\rightarrow \pi$  on input $σ$ , a statement $x$  and witness $m$ ,outputs an argument $\pi$ 

$-\text {Verify}(σ,\mathrm {vk},x,\pi )\rightarrow 1/0$ : on input $σ$ , the private verification key $vk$ , a state-ment $x$  and a proof $\pi$ , it outputs either 1 indicating accepting the argument or 0 for rejecting it.

**Definition** **4** **(SNARK).** A triple of polynomial time algorithms **(Setup,Prove,** **Verify)** is a SNARK for an NP relation $R$ , if the following properties are sat-isfied:

·Completeness. For all $(x,w)\in \mathcal {R},$ the following holds:

$$\text {Pr}\left[\begin{array}{c}(σ,vk)\leftarrow \text {Setup}\left(1^{λ}\right);\pi \leftarrow \text {Prove}(σ,x,w):\\ \text {Verify}(σ,vk,x,\pi )=1\end{array}\right]=1$$

·Knowledge Soundness. For any PPT adversary $A$ , there exists a PPT algo-rithm $\mathcal {E}_{\mathcal {A}}$  such that the following probability is negligible in $λ$ :

$$\text {Pr}\left[\begin{array}{c}(σ,vk)\leftarrow \text {Setup}\left(1^{λ}\right);\left((\tilde {x},\tilde {\pi });w^{\prime }\right)\leftarrow \mathcal {A}|\mathcal {E}_{\mathcal {A}}(σ):\\ \text {Verify}\left(σ,vk,\tilde {x},\tilde {\pi }\right)=1\wedge \mathcal {R}\left(\tilde {x},w^{\prime }\right)=0\end{array}\right]$$

·A non-interactive argument of knowledge satisfies knowledge is succinct if the size of proof $\pi$  and the time to **Verify** are sublinear in the size of the statement proven.

If a SNARK does not require a private verification key $vk$ , then the SNARK scheme is referred to as a public-key SNARK. If a SNARK does not require a CRS $σ$ , then the SNARK scheme is referred to as transparent.

### 2.3 Polynomial Commitment Scheme

We adapt the definition from Brakedown [22].

**Definition 5 (Polynomial Commitment Scheme).** A polynomial commit-ment scheme for multilinear polynomial over Galois Ring $GR\left(p^{s},r\right)$   is a tuple of four protocolsPC=(Gen,Commit,Open,Eval):

· $pp\leftarrow \text {Gen}\left(1^{λ},μ\right)$  takes as input $μ$  (the number of variables in a multilinear polynomial); produces public parameters pp.

· $\mathcal {C}\leftarrow \text {Commit}(pp,\mathcal {G},γ$ :takes any input a $μ-variate$  multilinear polynomial over a Galois Ring $\mathcal {G}\in \text {Pol}\left(GR\left(p^{},\right),μ\right)$  and a random string $γ$  produces a commitment C.

<!-- Transparent SNARKs over Galois Rings -->

<!-- 11 -->

· $b\leftarrow \text {Open}(pp,\mathcal {C},\mathcal {G},γ)$ : verifies the opening of commitment $C$  to the $μ-variate$ multilinear polynomial $\mathcal {G}\in \text {Ply}\left(GR\left(p^{},\right),μ\right)$ ;outputs $b\in \{0,1\}$ 

· $b\leftarrow \text {Eval}(pp,\mathcal {C},z,v,μ,\mathcal {G})$  is a protocol between a PPT prover $P$  and verifier $V$ . Both $V$  and $P$  hold $a$  commitment C, the number of variables $μ$   a scalar $v\in GR\left(p^{s},r\right)$ $\text {and}z\in GR\left(p^{s},r\right)^{μ}$ . P additionally knows a $μ-variate$  mul-tilinear polynomial $\mathcal {G}\in \text {ol}\left(GR\left(p^{},\right),μ\right)$ . $P$  attempts to convince $V$  that $\mathcal {G}(z)=v$ . At the end of the protocol, $V$  outputs $b\in \{0,1\}$ 

A tuple of four protocol $(Gen,Commit,Open,Eval)$   is an extractable polyno-mial commitment scheme for multilinear polynomials over Galois Ring $GR\left(p^{s},r\right)$ if the following conditions hold.

**·Completeness.** For any multilinear polynomial $\mathcal {G}\in \text {Pol}\left(GR\left(p^{s},\right),μ\right)$ 

$$\text {Pr}\left[\begin{array}{c}pp\rightarrow \text {Gen}\left(1^{λ},μ\right);\mathcal {C}\leftarrow \text {Commit}(pp,\mathcal {G},γ):\\ \text {Eval}(pp,\mathcal {C},r,v,μ,\mathcal {G})=1\wedge v=\mathcal {G}(r)\end{array}\right]\geq 1-\text {negl}(λ)$$

**·Binding.** For any PPT adversary $\mathcal {A}$ , size parameter $μ\geq 1,$ 

25 $\left[\begin{array}{c}pp\leftarrow \text {Gen}\left(1^{λ},μ\right);\mathcal {C},\mathcal {G}_{0},\mathcal {G}_{1}\leftarrow \mathcal {A}(pp);\\ b_{0}\leftarrow \text {Open}\left(pp,\mathcal {C},\mathcal {G}_{0},γ\right);b_{1}\leftarrow \text {Open}\left(pp,\mathcal {C},\mathcal {G}_{1},γ\right);\\ b_{0}=b_{1}\neq 0\wedge \mathcal {G}_{0}\neq \mathcal {G}_{1}\end{array}\right]\leq \text {negl}(λ)$ 

**·Knowledge** **soundness.** Eval is a succinct argument of knowledge for the following $NP$ relation given $pp\leftarrow \text {G}\left(1^{λ},μ\right)$ 

R $\text {Eval}(pp)=\left\{\langle (\mathcal {C},z,v),(\mathcal {G})\rangle :\mathcal {G}\in GR\left(p^{s},r\right)[μ]\wedge \mathcal {G}(z)=v\wedge \text {Open}(pp,\mathcal {C},\mathcal {G},γ)=1\right\}$ 

# 3 Linear Codes over Galois Rings

In SNARKs, the encoding is typically defined over a large prime field $\mathbb {F}_{q},$ as this ensures a large codeword distance and provides the verifier with a large challenge space. However, in the case of Galois rings, zero divisors can disrupt the code distance. In this section, we solve this problem and show expandler codes over arbitrary Galois rings.

## 3.1 Linear Codes over Partial Galois Rings

We utilized the construction framework of expander codes from Brakedown [22] and use a similar approach when proving the code distance.When analyzing codeword distances, for lemmas (Lemma 2 and Lemma 3) that are applicable to large prime fields but not to Galois rings, we provide versions of these lemmas for Galois rings(GR $\left(p^{}r\right)$ and their proofs. We use the same encoding parameters as in Brakedown. While the failure probability in Brakedown does not exceed $2^{-100}$ ,we proved that in Galois rings $\mathrm {GR}\left(p^{s},r\right)$ ,the error probability does not exceedS· $2^{-100}$ . In practical applications, $S$  typically does not exceed $2^{8}$ ,so our scheme remains secure.

<!-- 12 -->

<!-- Yuanju Wei, Xinxuan Zhang, and Yi Deng -->

Firstly, we need to define some parameters related to expander codes in Brakedown: $0<α<1,0<\beta <1,$ and $t>\frac {1+2\beta }{1-α}$ ,with $c_{n},d_{n}\geq 3.$  Let $\mathcal {M}_{n,m,d}\subset \text {GR}\left(p^{s},r\right)^{nxm}$ be a matrix distribution where each row has exactly $d$  non-zero entries, with these $d$  entries randomly selected from the non-zero elements of GR $\left(p^{},r\right)$ .For $x\in [0,1],$ $H(x)=-x\log _{2}(x)-(1-x)\log _{2}(1-x)$ 

$$c_{n}=\left\lceil \min (\max (1.28\beta n,\beta n+4)\right.$$

$$\left.\left.\frac {1}{\beta \log _{2}\left(\frac {α}{1.28\beta }\right)}\left(\frac {100}{n}+H(\beta )+αH\left(\frac {1.28\beta }{α}\right)\right)\right)\right]$$

$$d_{n}=\left\lceil \min \left(\left(2\beta +\frac {(t-1)+100/n}{\log _{2}\left(p^{r}\right)}\right)n,D\right)\right\rceil$$

$$D=\max \left(\frac {tαH\left(\frac {\beta }{t}\right)+μH\left(\frac {ν}{μ}\right)+\frac {100}{n}}{α\beta \log _{2}\left(\frac {μ}{ν}\right)},\right.$$

$$\frac {tαH\left(\frac {\beta }{t}\right)+μH\left(\frac {2\beta +0.03}{μ}\right)+\frac {100}{n}}{\beta \log _{2}\left(\frac {μ}{2\beta +0.03}\right)},$$

$$\left.(2\beta +0.03)\left(\frac {1}{αt-\beta }+\frac {1}{α\beta }+\frac {1}{μ-2\beta -0.03}\right)+1\right)$$

where $μ=t-1-tα,ν=\beta +α\beta +0.03.$ 

**Algorithm 1:** Enc Algorithm: $\text {GR}\left(p^{s},r\right)^{n}\rightarrow \text {GR}\left(p^{s},r\right)^{tn}$ 

**Input**: $\mathbf {x}\in \text {GR}\left(p^{s},r\right)^{n}$ 

**parameter:** $α,\beta ,t,c_{n},d_{n}$ 

**Output**: $\mathbf {w}\in \text {GR}\left(p^{s},r\right)^{tn}$ 

1 if $n<n_{0}$ **then**

2 $\mathrm {w}=M\mathbf {x}$ and return $W$ ;

3 Matrices $A^{(n)}\leftarrow \mathcal {M}_{n,αn,c_{n}}$ and $B^{(n)}\leftarrow \mathcal {M}_{αtn,(t-1-tα)n,d_{n}}$ for are choosen in pre-processing;

4 $\mathbf {y}^{\top }=\mathbf {x}^{\top }·A^{()}\in \text {GR}\left(p^{s},r\right)^{α}$ 

5 $\mathbf {z}=\text {Enc}(\mathbf {y})\in \text {GR}\left(p^{s},r\right)^{tαn};$ 

6 $\mathbf {v}^{\top }=\mathbf {z}^{\top }·B^{(n)}\in R^{(t-1-tα)n};$ 

$$\mathbf {7}\mathbf {w}=\left(\begin{array}{c}\mathbf {x}\\ \mathbf {z}\\ \mathbf {v}\end{array}\right)\in \mathrm {GR}\left(p^{s},r\right)^{tn}$$

8 return w

Here, $n_{0}$ is a small constant and M can be any generator matrix that meets the code rate and distance parameters, such as selecting an Reed-Solomon code and using padding zeros to meet the requirements. The Algorithm 1 is the en-coding algorithm over Galois rings. We prove that by choosing $α$ ,, $\beta$ , $t$  $c_{n}$ and $d_{n}$ with the same parameters as in the Brakedown scheme, it can still ensure the relative code distance over Galois rings with a probability of $1-\text {negl}(λ)$  .

<!-- Transparent SNARKs over Galois Rings -->

<!-- 13 -->

Since the proof framework for the relative code distance of expander codes over Galois rings is similar to that in Brakedown[22], we only highlight the differ-ences specific to Galois rings here. The complete proof is provided in Appendix B.

$E_{n,k}^{(1)}$  $\cdot$ Define the four events $E_{n,k}^{(2)}$ $E_{n,k}^{(3)}$ $and$ $E_{n,k}^{(4)}$ 

As explained in Brakedown [22], as long as the probability of these fouir events occurring does not exceeds· $2^{-100}$ ,Then, the relative code distance holds with a probability of $1-·2^{-100}$ 

· $E_{n,k}^{(1)}$ :There exists a set of $k$  coordinates of $\mathbf {x}\in \text {GR}\left(p^{s},r\right)^{n}$ that doesn't “expend” into $b(k)=\max (k+4,1.28k)\text {coordinatesof}$ $\mathbf {x}^{\top }·A.$ 

· $E_{n,k}^{(2)}$ : Given the event that, given that every set of size $k$  expands into a set of size at least $b(k)$  (that is, conditioned on the complement of $\left.E_{n,k}^{(1)}\right)$ ,there exists an x of Hamming weight $\|\mathbf {x}\|_{0}=h$ such that $\mathbf {y}^{\top }=\mathbf {x}^{\top }·A=\mathbf {0}$ 

· $E_{,k}^{(3)}$ : There exists a set of $k$  coordinates of $N$ $\in \text {GR}\left(p^{s},r\right)^{αrn}$  that doesn't expand into $b^{\prime }(k)=\left(\beta +k/n+\frac {(r-1)+110/n}{\log _{2}q}\right)$ 3 coordinates of $\mathbf {v}^{\top }=\mathbf {z}^{\top }$ $B.$ 

· $E_{n,k}^{(4)}$ : Given that all sets of size $k$  expand into at least $b^{\prime }(k)$  coordinates,there exists $a$ $\mathbf {z}\in \text {GR}\left(p^{s},r\right)^{αrn}$ of Hamming weight $\|\mathbf {z}\|_{0}=k$ which is mapped to $\mathbf {v}^{\top }=\mathbf {z}^{\top }$  $B$ of Hamming weight $\|\mathbf {v}\|_{0}<\beta n.$ 

Next,we demonstrate that by selecting an appropriate parameter $r$ ,the probability of the event $E_{,k}^{(2)}$ occurring remains below $s·2^{-100}$ .

**Lemma** 2. For every $n\leq 2^{30}$  and every Galois Ring( $GR\left(p^{},r\right)$  satisfying $p^{r}\geq$ $2^{127}$ ,for every n and $k\leq n$ $\text {Pr}\left[E_{n,k}^{(2)}\right]\leq ·2^{-100}$ 

Proof. Let $X$  be an element in(GR $\left(p^{s},r\right)^{n}$  with $\|\mathbf {x}\|_{0}=k$ ,and let $K\subseteq$ [n] represent the indices of the nonzero elements in x. Let $T_{K}$ be a random variable denoting the number of columns of $A\leftarrow \mathcal {M}_{,α,c_{}}$  with at least one non-zero element in the rows with indices from K.

The occurrence of the event $E_{,k}^{(2)}$ depends on the non-occurrence of the event $E_{n,k}^{(1)},$ we have that $T_{k}\geq b(k)=\max (k+4,1.28k).$ .We have at least $T_{k}$ coordi-nates of $A\mathbf {x}$  are non-zero linear combinations of the non-zero coordinates of x with indices form $K_{\mathbf {x}}.$ 

Here, a more complicated analysis than that used in the Brakedown scheme is necessary. Drawing from the Fact 2, we know that for any two elements $a$ and $b$  in the Galois ring $GR$ $\left(p^{s},r\right)$  $if$ $d=\text {gcd}\left(p^{s},a\right)$ ,then the linear equation $ax=b$ has at most $d^{r}$ solutions. If we treat the coefficients of the randomly selected linear combination as a variable $x$ , and one of the $k$  non-zero elements as $a$ , then the condition that the random linear combination equals zero means that for some $b$ , the equation $ax=b$ hold. According to Fact 2, the proportion

<!-- 14 -->

<!-- Yuanju Wei,Xinxuan Zhang, and Yi Deng -->

of $x$  values that satisfy this condition is $\frac {d^{r}}{p^{sr}}$ ,which implies that the probability of the random linear combination being zero does not exceed $\frac {d^{r}}{p^{sr}}$ . It is noted that the likelihood of a random linear combination being zero is influenced by the element with the smallest greatest common divisor with $p^{s}$ .Therefore,we can categorize and discuss based on the smallest greatest common divisor of $k$ elements. Assuming that the $k$  nonzero elements in $X$  have the smallest greatest common divisor with $p^{s}$ as $d$ , the probability that a random linear combination is zero is $\frac {d^{}}{p^{s}}$ . There are $b(k)$  positions in $\mathbf {x}^{\top }$ ·A that are nonzero elements for the linear combination, thus the probability that all arezerois $\left(\frac {d^{r}}{p^{sr}}\right)^{b(k)}$ Given that $X$  has $k$  nonzero elements, and the greatest common divisor of these nonzero elements with $p^{s}$  does not exceed $d$ , the number of potential selections is $\binom {k}{n}·\left(\left(\frac {p^{}}{d}\right)^{r}\right)^{k}$ . Consequently, when the $k$  nonzero elements in $x$  have the smallest common factor with $p^{s}$  as $d$  , the probability of event $E_{n,k}^{(2),d}$ occurring does not exceed:

$$\left(\frac {d^{r}}{p^{sr}}\right)^{b(k)}·\left(\left(\frac {p^{s}}{d}\right)^{r}\right)^{k}·\binom {k}{n}=\binom {k}{n}\left(\frac {d^{b(k)-k}}{p^{s(b(k)-k)}}\right)^{r}$$

It is evident that the larger the value of $d$ , the greater the probability of error. When $d$  reaches its maximum at $p^{-1}$ ,the error probability attains its highest value of:

$$\binom {k}{n}\left(\frac {1}{p^{b(k)-k}}\right)^{r}=\binom {k}{n}\left(\frac {1}{p^{r}}\right)^{b(k)-k}.$$

for $k\geq 15$ $n\leq 2^{30}$ ,and $p^{r}>2^{127}$ 

$$\text {Pr}\left[E_{n,k}^{(2),d}\right]\leq \binom {k}{n}\left(\frac {1}{p^{r}}\right)^{b(k)-k}\leq \binom {k}{n}\left(p^{r}\right)^{-0.28k}\leq \left(\frac {en}{k}\right)^{k}·\left(p^{r}\right)^{-0.28k}\quad =\left(\frac {en}{k\left(p^{r}\right)^{0.28}}\right)^{k}\leq 2^{-120}$$

for $k\leq 14$ $n\leq 2^{30}$ ,and $p^{r}>2^{127}$ 

$$\text {Pr}\left[E_{n,k}^{(2),d}\right]\leq \binom {k}{n}\left(\frac {1}{p^{r}}\right)^{b(k)-k}\leq \binom {k}{n}\left(p^{r}\right)^{-4}\leq \left(\frac {en}{k}\right)^{k}·\left(p^{r}\right)^{-4}\quad =\left(\frac {en}{14}\right)^{14}\leq 2^{-120}$$

Of course, this scenario pertains to when d attains its largest value.The maximum number of possible values for d is s, which includes 1, $p,$ $···,$ $p^{-1}$ According to the union bound, the soundness error can increase by at most s times. In this case, the error probability remains below $\frac {s}{2^{100}}$ ,a value which is still negligible. ☐

**Lemma** **3.** For every n and $α\beta n\leq k\leq \beta n$ $\text {if}2\beta +\frac {(t-1)+110/n}{\log _{2}\left(p^{r}\right)}\leq t-1-tα$ $\text {Pr}\left[E_{n,k}^{(4)}\right]\leq s·2^{-100}$ 

<!-- Transparent SNARKs over Galois Rings -->

<!-- 15 -->

Proof. Assuming that every set of size $k$  expands to at least

$$b^{\prime}(k)=\left(\beta+\frac{k}{n}+\frac{(t-1)+\frac{110}{n}}{\left.\log _{2}\left(p^{r}\right)\right)}\right)n$$

we need to demonstrate that, with overwhelming probability, every element $z$  in GR $\left(p^{s}r\right)^{αn}$  with a Hamming weight of $\|z\|_{0}=k$ can be mapped to a vector $\mathbf {v}=B$ ·z in( $\text {GR}\left(p^{s},r\right)^{(-1-α)n}$ with a Hamming weight $\|\mathbf {v}\|_{0}\geq \beta .$ 

Fix an element $Z$  in $\text {GR}\left(p^{s},r\right)$  with a Hamming weight $\|\mathbf {z}\|_{0}=k$ .Given that the non-zero coordinates of $Z$  will expand to at least $b^{\prime }(k)$  coordinates,there are at least $b^{\prime }(k)$  positions in the vector $v$  comprising random linear combinations of non-zero vectors. As in the previous proof, if the $k$  non-zero elements in $x$ have the smallest greatest common divisor with $p^{s}$ as $d$ , then the probability of a random linear combination resulting in zero is $\frac {d^{}}{p^{}}$ .Consequently,with $Z$  fixed, the probability that $\|\mathbf {v}\|_{0}\leq \beta n$ is bounded by:

$$\binom {b^{\prime }(k)}{\geq b^{\prime }(k)-\beta n}\left(\frac {d^{r}}{p^{sr}}\right)^{b^{\prime }(k)-\beta n}\leq \binom {(t-1-tα)n}{\geq (t-1-tα)n-\beta n}\left(\frac {d^{r}}{p^{sr}}\right)^{b^{\prime }(k)-\beta n}$$

$$\leq 2^{(t-1-tα)n}\left(\frac {d^{r}}{p^{sr}}\right)^{b^{\prime }(k)-\beta n}$$

assuming $b^{\prime}(k)\leq\left(2\beta+\frac{(t-1)+\frac{110}{n}}{\left.\log _{2}\left(p^{r}\right)\right)}\right)$ $)n.$ -For all $Z$  in GR $\left(p^{s},\right)^{αt}$ that satisfy $\|\mathbf {z}\|_{0}=k$ and whose greatest common divisor with $p^{s}$ is $d,$ the upper bound is given by:

$$\text {Pr}\left[E_{n,k}^{(4),d}\right]\leq \binom {tαn}{k}\left(\left(\frac {p^{s}}{d}\right)^{r}\right)^{k}·2^{(t-1-tα)n}·\left(\frac {d^{r}}{p^{sr}}\right)^{b^{\prime }(k)-\beta n}\quad =\binom {tαn}{k}·2^{(t-1-tα)n}·\left(\frac {d^{r}}{p^{sr}}\right)^{b^{\prime }(k)-\beta n-k}.$$

It is evident that the larger the value of $d$ , the greater the probability of error. When d reaches its maximum at $p^{-1}$ ,the error probability attains its highest value of:

$$\binom {tαn}{k}·2^{(t-1-tα)n}·\left(\frac {1}{p^{r}}\right)^{b^{\prime }(k)-\beta n-k}$$

$$\leq \frac {2^{(r-1)n}}{\left(p^{r}\right)^{\left(\frac {(t-1+100/n)}{\log _{2}\left(p^{r}\right)}\right)n}}«2^{-100}$$

$$\text {Pr}\left[E_{n,k}^{(4),d}\right]\leq \binom {tαn}{k}·2^{(t-1-tα)n}·\left(\frac {1}{p^{r}}\right)^{b^{\prime }(k)-\beta n-k}\leq \frac {2^{tαn+(t-1-tα)n}}{\left(p^{r}\right)^{b^{\prime }(k)-\beta n-k}}$$

Of course, this scenario pertains to when d attains its largest value. The maximum number of possible values for d is s, which includes1, $p\cdots p^{-1}$ 

<!-- 16 -->

<!-- Yuanju Wei, Xinxuan Zhang,and Yi Deng -->

According to the union bound, the soundness error can increase by at most $S$ times. In this case, the error probability remains below $\frac {s}{2^{100}}$ ,a value which is still negligibole. ☐

## 3.2 Linear Codes over Extensions of Small Galois Rings

In the first part of this section, the encoding was designed for rings $\mathrm {GR}\left(p^{s},r\right)$ where $p^{r}$ is sufficiently large. However, if $p^{r}$ is not exponentially large relative to the security parameter $λ$ ,what can be done?If the parameter $p^{r}$ in a Galois ring requires a $k-fold$ extension such that $p^{k}$ becomes exponentially large with respect to $λ$ , a trivial solution is to perform a $k-fold$  expansion by padding each element of $\mathrm {GR}\left(p^{s},r\right)$  into $\mathrm {GR}\left(p^{s},kr\right)$ . However, this padding method is inefficient.

Inspired by the block-level coding approach from the Binius scheme [17],we treat $k$  consecutive elements from G $\mathrm {R}\left(p^{s},r\right)$ as a single element in $\mathrm {GR}\left(p^{s},kr\right)$ for encoding, resulting in higher encoding efficiency.

In [32], Theorem 14.23 proves that the extension of any Galois ring is still a Galois ring.

**Claim 1 ([32] Theorem 14.23)** For any Galois ring $R=GR\left(p^{s},r\right),h(x)$  is a monic irreducible polynomial of degree $k$  over R. Then the residue class ring $R[x]/(h(x))$  is a Galois ring of characteristic $p^{s}$ and it has $p^{k}$  elements and contains R as a subring. Thus

$$R[x]/(h(x))=GR\left(p^{s},kr\right)$$

Through this claim, we can conclude that the extension of a Galois ring remains a Galois ring.

In this paper,if the Galois ring $G_{1}=\text {GR}\left(p^{s},r\right)$ does not satisfy the require-ment that $p^{r}$ is large enough, and an extension $G_{2}=\text {GR}\left(p^{s},kr\right)$ is needed. At this time, $G_{2}$ is obtained as an extension of $G_{1}$  by a $degree-k$  irreducible polynomial. Elements of $G_{2}$ can be viewed as polynomials of degree $k-1$  with coefficients in $G_{1}$ .Therefore, an element of $G_{2}$  can be represented as a vector consisting of $k$  elements from $G_{1}$ . The multiplication of an element $a$  from $G_{1}$ with an element $b$  from $G_{2}$ can be interpreted as performing the multiplication of $a$  with each entry of the length- $k$  vector corresponding to $b$ . In this paper,the relationship holds for the corresponding Galois rings GR $\left(^{},\right)$ and $\text {GR}\left(p^{s},kr\right)$ 

From Fact 4, we conclude that if a monic polynomial is irreducible over GF $(p,r)$ ,then it remains irreducible over GF( $\left(p^{},\right)$ .Therefore, we can search for irreducible polynomials over the Galois field corresponding to the Galois ring. The method for finding irreducible polynomials of degree $d$  over a general Galois field has already been provided in [6].

For small Galois rings, the encoding involves two Galois rings. Below is the definition of the $[l,n,d]-k-GR(ps,r)code.$ 

**Definition 6.** $([l,n,d]-k-GR\left(p^{s},r\right)$  Code) For a vector of length kn over Ga-lois Ring( $GR\left(p^{},r\right)$ ,treat each $k$  consecutive elements as a single element in

<!-- Transparent SNARKs over Galois Rings -->

<!-- 17 -->

$GR\left(p^{s},\right.$ kr)(we treat the adjacent $k$  elements ofC $GR\left(p^{s},r\right)$  as a k-dimensional vector.),where the parameters p, $k$ , and $r$  satisfy the condition that $\frac {1}{p^{kr}}$ is neg-ligible relative to the security parameter $λ$ . The generator matrix G is an $n$ matrix defined over( $GR\left(p^{s},kr\right)$ .After encoding, the result is a vector of length l over $GR\left(p^{s},kr\right)$ . Furthermore, for any two distinct vectors of length kn over $GR\left(p^{s},\right)$ ,at least d elements in $GR\left(p^{s},kr\right)$ ,kr)are different after encoding.

### Algorithm 2: Enc' $\text {AlgorithGR}\left(p^{s},r\right)^{kn}\rightarrow \text {GR}\left(p^{s},kr\right)^{tn}$ 

**Input**: $\mathbf {x}\in \text {GR}\left(p^{s},r\right)^{kn}$ 

**parameter:**α,β,t, $c_{n},d_{n}$ 

**Output**: $\mathbf {w}\in \mathrm {GR}\left(p^{s},kr\right)^{tn}\left(\mathrm {GR}\left(p^{s},kr\right)\right.$ is obtained as an extension of $\text {GR}\left(p^{s},r\right)$ by a degree- $k$  irreducible polynomial.)

1 Treat the adjacent $k$  elements of $\mathrm {GR}\left(p^{s},r\right)$  as a k-dimensional vector. According to Claim 1, a $k\text {-length}$ $\mathrm {GR}\left(p^{s},r\right)$  vector is considered an element of $\text {GR}\left(p^{s},kr\right)$ . This leads to treating the vector x of length kn into a vector $\mathbf {x}^{\prime }$ ' of length $n$  over GR $\left(p^{s},kr\right)$ 

2 Call $\text {Enc}\left(\mathbf {x}^{\prime }\right)$ to obtain w and output w;

It can be observed that the encoding over GR $\left(p^{s},\right)$ is essentially an encod-ing over $\text {GR}\left(p^{s},kr\right)$ . However, we must demonstrate that Enc' supports linear combinations over $\mathrm {GR}\left(p^{s},r\right)$ ;otherwise, it cannot be considered a linear code over GR $\left(p^{s},\right)$ 

### Lemma 4. Enc' supports linear combinations over( $GR\left(p^{},\right)$ 

Proof. One important point to note is that an element of GR $\left(p^{},kr\right)$ $\{\}^{\mathrm {心}}$  can be viewed as a vector of $k$  elements over $\mathrm {GR}\left(p^{s},r\right)$ .Let $a$ $\in \mathrm {GR}\left(p^{s},r\right)$ and $bE$ $\mathrm {GR}\left(p^{s},\right.$ kr). At the same time, $a$  can beconsidered as an element in GR( $R\left(p^{}kr\right)$ 

The multiplication $a·b$ in GR $\left(p^{s},kr\right)$ can be understood as the component-wise multiplication of $a$  with each element of $b$ . Therefore, the dot product of an element $a\in \text {GR}\left(p^{s}r\right)$ with a vector of length l over $\text {GR}\left(p^{s}k\right)$ can be viewed as the dot product of $a$  with a vector of length $lk$  over $\mathrm {GR}\left(p^{s},r\right)$ 

Let $a_{1},a_{2}\in \text {GR}\left(p^{s},r\right)$ ,and let $\mathbf {b}_{1},\mathbf {b}_{2}\in \text {GR}\left(p^{s},r\right)^{kn}$ ,with $\mathbf {b}_{1}^{\prime }$ and $\mathbf {b}^{\prime }{}_{2}$ being the vectors padded to length $n$  over GR $\left(p^{},k\right)$ .Then we have:

$$a_{1}·\text {Enc}^{\prime }\left(\mathbf {b}_{\mathbf {2}}\right)+a_{2}\quad ·\text {Enc}^{\prime }\left(\mathbf {b}_{\mathbf {2}}\right)=a_{1}\quad ·\text {Enc}\left(\mathbf {b}_{1}^{\prime }\right)+a_{2}\quad ·\text {Enc}\left(\mathbf {b}_{2}^{\prime }\right)$$

$$=\text {Enc}\left(a_{1}·\mathbf {b}_{1}^{\prime }+a_{2}·\mathbf {b}_{2}^{\prime }\right)$$

$$=\text {Enc}\left(a_{1}·\mathbf {b}_{\mathbf {1}}+a_{2}·\mathbf {b}_{\mathbf {2}}\right)$$

The second equality holds because Enc is a linear code defined over GR $\left(p^{},k\right)$ and $a\in \text {GR}\left(p^{s},kr\right)$ . Therefore, it can be concluded that Enc' supports linear combinations over GR $\left(p^{},\right)$  ☐

<!-- 18 -->

<!-- Yuanju Wei, Xinxuan Zhang, and Yi Deng -->

#### 3.3 Costs

Table 1 is the performance comparison for different parameters. While Brake-down applies its operations over a large 127-bit prime, our approach utilizes a Galois ring GR $\left(p^{}\right)$ . Additionally, in Brakedown, the probability of encoding failure does not exceed $2^{-100}$ ,while in the Galois ring (GR $\left(p^{s}r\right)$ ,the failure probability does not exceed s· $2^{-100}$ . In practical applications, $S$  is almost never greater than $2^{8}$ ,so the scheme remains secure over Galois rings.

Beyond those, the parametersα,β,t,( $c_{n}$ ,and $d_{n}$ can be selected in a man-ner consistent with those in the Brakedown scheme. If the encoding length is $n$ data from Brakedown suggest that, depending on the selected parameters, en-coding a vector of length $n$  will require between $13.2n$  to 25.5n multiplications on $\text {GR}\left(p^{s},r\right)$ .We denote this constant as $c_{0}$ ,with $c_{0}$ approximately ranging from 13.2 to 25.5. The cost of the expander code in this work is consistent with that in Brakedown, except for the difference in the basic computational unit.

Table 1:Linear Code over Galois rings( $\text {GR}\left(p^{},\right)$ performance according to Brakedown [22].

<table border="1" ><tr>
<td>n</td>
<td>$p^{r}$</td>
<td>Pr[failure]</td>
<td> Run-time </td>
<td>Distanace</td>
<td> Rate</td>
<td>$α$</td>
<td>$\beta$</td>
<td>$t$ $c_{n}d_{n}$</td>
<td></td>
</tr><tr>
<td>$\leq 2^{30}\geq 2^{127}<s·2^{-100}$ $\leq 2^{30}\geq 2^{127}<s·2^{-100}$</td>
<td></td>
<td></td>
<td>13.2n</td>
<td>0.02</td>
<td>0.704</td>
<td> 0.1195</td>
<td> 0.0284</td>
<td> 1.42 </td>
<td>6 33</td>
</tr><tr>
<td></td>
<td></td>
<td></td>
<td>14.3n</td>
<td>0.03</td>
<td>0.68</td>
<td> 0.138</td>
<td>0.0444</td>
<td> 1.47</td>
<td> 7 26</td>
</tr><tr>
<td>$\leq 2^{30}\geq 2^{127}<s·2^{-100}$</td>
<td></td>
<td></td>
<td>15.8n</td>
<td>0.04</td>
<td>0.65</td>
<td>0.178</td>
<td>0.061 </td>
<td>1.521</td>
<td> 7 22</td>
</tr><tr>
<td>$\leq 2^{30}\geq 2^{127}<s·2^{-100}$ $\leq 2^{30}\geq 2^{127}<s·2^{-100}$</td>
<td></td>
<td></td>
<td>$17.8n$</td>
<td>0.05</td>
<td>0.60</td>
<td>0.2</td>
<td>0.082</td>
<td> 1.64 </td>
<td>8 19</td>
</tr><tr>
<td>$\leq 2^{30}\geq 2^{127}<s·2^{-100}$</td>
<td></td>
<td></td>
<td>20.5n</td>
<td>0.06</td>
<td>0.61</td>
<td>0.211</td>
<td>0.097 </td>
<td>1.616</td>
<td> 9 21</td>
</tr><tr>
<td></td>
<td></td>
<td></td>
<td>25.5n</td>
<td>0.07</td>
<td>0.58</td>
<td>0.238</td>
<td>0.1205</td>
<td> 1.72 </td>
<td>10 23</td>
</tr></table>

# 4 Linear Time Polynomial Commitment

The Brakedown polynomial commitment relies on the lemma from AHIV17[1], so our first step is to prove that this lemma holds over arbitrary Galois rings. So we must consider the case where polynomial coefficients lie in a small ring. Recall that during encoding, we treat $k$  elements from the small ring as a single element from the extension ring. If the verifier directly selects a challenge $e$  from the extension ring, then e will multiply with the larger element formed by the $k$  elements from the small ring. So the adjacent $k$  coefficients will be mixed together.However, if the verifier selects challenges only from the small ring,the soundness cannot be guaranteed.

To solve this issue, we prove a “repetition” version of the AHIV17[1] lemma over arbitrary Galois rings. In the “repetition” version lemma, the verifier en-sures soundness by selecting multipole challenges from the small ring, while also preventing the mixing of the $k$  coefficients. To prove the “repetition” version lemma,it is necessary to utilize the definitions of Interleaved Codes and Block-wise Relative Distance.

<!-- Transparent SNARKs over Galois Rings -->

<!-- 19 -->

**Definition** $7$  **(Interleaved code and Block-wise relative distance [9]).** Let $C\subset \mathbb {F}^{n}$ be an $[n,k,d]$  llinear code over $F$ .We let $C^{m}$  denote the $[mn,mk,d]$ (interleaved) code over $\mathbb {F}^{m}$  whose codewords are all $mx$  matrices $A$  such that every row $A_{i}$ of $A$  satisfies $A_{i}\in C.$ .For $A\in C^{m}$  and $j\in [n],$  we denote by $A[j]$ ]the jth symbol of $A$ .

Moreover,we introduce the denifiniton of block-wise distance of $\left(A,C^{m}\right)$ :

$$d\left(A,L^{m}\right):=\frac {\left|\left\{j\in [n]|\exists i\text {s.t.}A_{i}[j]\neq c_{i}[j]\right\}\right|}{n}$$

where $c_{i}$ denote the closest codeword with $A_{i}$  in C.

**Definition 8 (Interleaved code and Block-wise relative distance over** **Galois Rings).** $\text {Lt}C\subset GR\left(p^{s},kr\right)^{}$ be an $[n,k,d]$  linear code over $GR\left(p^{s},kr\right)$ .We let $C^{m}$  denote the $[mn,mk,d]$   (interleaved) code over $GR\left(p^{s},k\right)^{m}$  whose codewords are all $m\times n$  matrices $A$  such that every row $A_{i}$ of A satisfies $A_{i}\in C.$ For $A\in C^{m}$  and $j\in [n]$ ,we denote by $A[j]$ the jth symbol of A.

Moreover, we give the denifiniton of the block-wise distanceof( $\left(A,C^{m}\right)$ 

$$d\left(A,L^{m}\right):=\frac {\left|\left\{j\in [n]|\exists i\text {s.t.}A_{i}[j]\neq c_{i}[j]\right\}\right|}{n}$$

where $c_{i}$  denote the closest codeword with $U_{i}$ in C.

**Claim 2 (Ames, Hazay, Ishai, and Venkitasubramaniam [1], Roth and Zémor)** Fix an arbitrary [n,k,d]-code $L$ $\subset \mathbb {F}_{q}^{n}$ ,and a proximity parameter $e\in \left\{0\cdots \left\lfloor \frac {-1}{3}\right\rfloor \right\}$ Supposed $d\left(U,C^{m}\right)>$ .Then for a random $w^{*}$  in the row-span $ofU,$ we have

$$\text {Pr}\left[d\left(w^{*},L\right)\leq e\right]\leq (e+1)/|\mathbb {F}|$$

The presence of zero divisors in the Galois ring GR $\left(p^{},r\right)$ increases the possi-bbility $\frac {e+1}{q}$ . To maintain this probability at a negligible level, we repeatedly select challenges to reduce it.

**Claim 3 (AHIV17 Repetition Version)**Fix any $k-[l,n,d]$ code $C\subset GR\left(p^{s}\right.$ $kr)^{l}$ over the Galois ring GR( $\left(p^{},\right)$ ,and a proximity parameter $e\in \left\{0,\cdots ,\left\lfloor \frac {-1}{3}\right\rfloor \right\}$ For a matrix $U\in GR\left(p^{},k\right)^{mxl}$ with $(U$ $\left.C^{m}\right)>e$ and a matrix $R\in GR\left(p^{s},\right.$ $)^{kxm}$  where each element of $R$  is randomly chosen from $GR\left(p^{},r\right)$ $\text {let}W=RU$ Then we have:

$$\text {Pr}\left[d\left(W,C^{k}\right)\leq e\right]\leq \frac {e+1}{p^{rk}}$$

We provide the proof of this claim in the Appendix C.1.

With the verification lemma established above, we can now give a polynomial commitment over the Galois ring $\text {GR}\left(p^{}\right)$ 

This polynomial commitment is designed to compute a multilinear poly-nomial $f\left(x_{1},\cdots ,x_{l}\right)$ defined over GR $\left(p^{},\right)$ ,where polynomial coefficient $x_{i}\in$ 

<!-- 20 -->

<!-- Yuanju Wei, Xinxuan Zhang, and Yi Deng -->

$\text {GR}\left(p^{s},r\right)$ .Assume that the encoding over $\mathrm {GR}\left(p^{s},r\right)$ uses a $k-[l,n,d]$ code,where $k\geq 1.$ 

## Polynomial Commitment for $\mathbf {GR}\left(p^{s},r\right)$ :

## - Commit Phase

·The prover splits the multilinear polynomial with l variables into a ma-trix U of size $2^{/2}x2^{/2}.\text {Lt}m=2^{/2}$  Each row of the mnatrix is encoded using a k-[tm,m,d] code to obtain a matrix $\hat {U}$ with $m$  rows, $\hat {U}_{1},\cdots ,\hat {U}_{m},$ where $\hat {U}_{i}\in \text {GR}\left(p^{s},kr\right)^{tm/k}$ ,and $\hat {U}_{i}[j$ represents the element in the i-th row and $j-th$ column of the matrixU.The prover constructs a Merkle Tree to commit to $\hat {U}$ .

## -Testing Phase

**·Verifier** $\rightarrow \mathbf {P}$ over: A random matrix $R\in \left(\text {GR}\left(p^{s},r\right)\right)^{kxm}$ ,where each element of R is randomly chosen from $\text {G}\left(p^{},r\right)$ 

·Prover $\rightarrow$  Verifier: Treat every $k$  adjacent elements in each row of matrix $U$ as an element in( $\mathrm {GR}\left(p^{},r\right)$ ,and compute $V=RU\in \mathrm {GR}\left(p^{}\right.$ $kr)^{k\times (m/k)}$ and calims that $V=RU$ .Then,send $A$  to the verifier.

·Verifier: Choose a random set $Q$  of size $l_{Q}=Θ(λ)$ ,with $Q\subseteq [\mathrm {tm}/k]$ For each $j\in Q:$ 

- Verifier queries all $m$  entries of the corresponding “column” of $\hat {U}$ $\hat {U}_{1}[j],\cdots ,\hat {U}_{m}[j]$ . The verifier receives these values and the associated Merkle Tree paths from the prover. If one of the paths is invalid, reject.

-For each row $V_{i}$ of matrixV,the verifier confirms that $\text {Enc}\left(V_{i}\right)[j]=$ $\sum _{s=1}^{m}R_{i}[s]·\hat {U}_{s}[j]$ ,rejecting if the condition is not satisfied.

## - Evaluation Phase

·To compute $f\left(r_{1},\cdots ,r_{l}\right)$ ,calculate $\mathbf {q}_{\mathbf {1}}=\left(1-r_{1},r_{1}\right)\otimes \cdots \otimes \left(1-r_{l/2},r_{l/2}\right)\in$ $\text {GR}\left(p^{s},r\right)^{m}$ and $\mathbf {q}_{\mathbf {2}}=\left(1-r_{l/2+1},r_{l/2+1}\right)\otimes \cdots \otimes \left(1-r_{l},r_{l}\right)\in \mathrm {GR}\left(p^{s},r\right)^{m}$ 

·The evaluation phase is similar to the testing phase, but with $R$ replaced by $\mathrm {q}_{1}$  to obtain $V$ .

·If all previous checks pass, the verifier treats v as a vector of length1 $m$ $\text {over}\mathrm {GR}\left(p^{s},r\right)$ ,and computes $f\left(r_{1},\cdots ,r_{l}\right)=\left\langle \mathbf {v},\mathbf {q}_{\mathbf {2}}\right\rangle$ 

## The Proof of Completeness

Proof. To demonstrate completeness, we first express the matrix $U$ as:

$$U=\begin{bmatrix}f(0,\cdots ,0,0,\cdots ,0)\;f(0,\cdots ,0,0,\cdots ,1)\cdots f(0,\cdots ,0,1,\cdots ,1)\\ f(0,\cdots ,1,0,\cdots ,0)\;f(0,\cdots ,1,0,\cdots ,1)\cdots f(0,\cdots ,1,1,\cdots ,1)\\ :&:&:\\ f(1,\cdots ,1,0,\cdots ,0)\;f(1,\cdots ,1,0,\cdots ,1)\cdots f(1,\cdots ,1,1,\cdots ,1)\end{bmatrix}$$

To compute $f\left(r_{1},\cdots ,r_{l}\right)$ ,we use the formula:

$$f\left(r_{1},\cdots ,r_{l}\right)=\sum _{\mathbf {b}\in \{0,1\}}\prod _{i\in [1,l]}\left(\left(1-r_{i}\right)\left(1-b_{i}\right)+r_{i}b_{i}\right)f(\mathbf {b})$$

<!-- Transparent SNARKs over Galois Rings -->

<!-- 21 -->

which can be rewritten as:

$$f\left(r_{1},\cdots ,r_{l}\right)=\mathbf {q}_{1}^{\top }U\mathbf {q}_{2}.$$

An important observation is that if we treat the $i$ -th row of the matrix $U$ ,denoted $U_{i}$  as $m/k$  elements $w_{1},\cdots ,w_{m/k}$  $inGR(ps,kr)$ ,then for $a\in \text {GR}\left(p^{s},r\right)$ ,we have:

$$a·\left(w_{1},\cdots ,w_{m/k}\right)$$

$$=\left(a·w_{1},\cdots ,a·w_{m/k}\right)$$

$$=\left(a\left(w_{1,1},\cdots ,w_{1,k}\right),\cdots ,a\left(w_{(m/k),1},\cdots ,w_{(m/k),k}\right)\right)$$

$$=\left(aw_{1,1},\cdots ,aw_{1,k},\cdots ,aw_{(m/k),1},\cdots ,aw_{(m/k),k}\right)$$

$$=a·\left(U_{i}[1],\cdots ,U_{i}[m]\right)$$

Since each $w_{i}$  can be viewed as $k$  elements from GR $\left(p^{s},r\right)$ ,the compu-tation is the same whether we treat U as a matrix in GR(ps, $)^{mm}$  or in GR $\left(p^{},k\right)^{m(m/k)}$ .Thus,the result of $\mathbf {q}_{1}^{\top }$  remains the same in “form” re-gardless of the interpretation. Therefore, the linear combination of the matrix can be seen as a linear combination over GR $\left(p^{},\right)$ ,ensuring the completeness of the polynomial commitment. ☐

## The Proof of Soundness

Proof. The encoded coefficient matrix $\hat {U}\in \text {GR}\left(p^{s},kr\right)^{m\times (m/k)}$ ,where $N=m/k,$ has a code distance of d and a relative distance of $γ=d/N.$ $-$ .First,we show that if the prover can pass the testing phase with a probability greater than $\frac {1}{p^{k}}+\left(1-\frac {γ}{3}\right)^{l_{Q}}$ ,then there must exist a codeword $\left[c_{1},\cdots ,c_{m}\right]\in C^{m}$ such that:

$E:=|\{j\in [N]:\exists i\in [m]$ ,such that $\left.\left.c_{i,j}\neq \hat {U}_{i,j}\right\}\right|\leq \left(\frac {γ}{3}\right)N.$ 

Let $e=\left\lfloor \frac {γN}{3}\right\rfloor$ .If no such codeword $\left[c_{1},\cdots ,c_{m}\right]$ exists,then we have $d\left(\hat {U},C^{m}\right)>$ e. According to claim 3,since $V=R·\hat {U}$ ,the probability that $d\left(V,C^{k}\right)$ is greater than $e$  is at least $1-\frac {e+1}{p^{kr}}$ 

Thus,during the testing phase, the probability that the verifier randomly selects $l$  columns without hitting any columns in $\Delta \left(VC^{k}\right)$ is at most $\left(1-\frac {}{3}\right)^{l_{Q}}=$ $\left(1-\frac {γ}{3}\right)^{l_{Q}}$ .Define the event $E_{1}$ $\text {as}d\left(V,C^{k}\right)>e$ and the event $E_{2}$ as the verifier selecting $l$  columns without hitting any in $\Delta \left(V,C^{k}\right)$ .Then,we have:

$$\text {Pr}\left[P^{*}\text {wins}\right]\leq \text {Pr}\left[P^{*}\text {wins}|\bar {E}_{1}\right]\text {Pr}\left[\bar {E}_{1}\right]+\text {Pr}\left[P^{*}\text {wins}|E_{1}\right]\text {Pr}\left[E_{1}\right]$$

$$\leq \text {Pr}\left[\bar {E}_{1}\right]+\text {Pr}\left[E_{2}|E_{1}\right]\text {Pr}\left[E_{1}\right]$$

$$<\frac {1}{p^{kr}}+\text {Pr}\left[E_{2}|E_{1}\right]=\frac {1}{p^{kr}}+\left(1-\frac {γ}{3}\right)^{l_{Q}}$$

This contradicts the fact that the prover could pass the testing phase, so there must exist such a codeword $c_{1},\cdots ,c_{m}$ 

<!-- 22 -->

<!-- Yuanju Wei, Xinxuan Zhang, and Yi Deng -->

We can observe that each $c_{i}$ is the closest codeword to each row $U_{i}$ of $\hat {U};$ otherwise, the distance between two codewords woould be less than $d$ . Define $w:=\sum _{i=1}^{m}q_{1,i}·c_{i}.\text {Next}$ ,we show thatif the prover can pass the testing phase with a probability greater than $\frac {e+1}{p^{k}}+\left(1-\frac {d}{3}\right)^{l_{Q}}$ , and pass the evaluation phase with a probability greater than $\left(1-\frac {2γ}{3}\right)^{l_{Q}}$ ,then let $u:=\sum _{i=1}^{m}q_{1,i}·U_{i}\text {ad}w=\text {Ec}(u)$ 

If $w\neq \text {Enc}(u)$ , then $m$  and $\text {Enc}(v)$  are two distinct codewords in C,meaning that the number of positions where they are identical will not exceed $(1-γ)N.$ Let the set of positions where the two codewords are identical be denoted1 as A. If the verifier selects a column $j\notin A\cup E$ ,then the test will fail because $m$  and Enc(u) can only match within $A\cup E$ . We have $|A\cup E|\leq |A|+|E|\leq$ (1 $-γN+\left(\frac {γ}{3}\right)N=\left(1-\frac {2γ}{3}\right)$ $N$ . Since the verifier selects the set of columns $Q$  randomly, if the prover is cheating, the probability of passing the evaluation phase will not exceed $\left(1-\frac {2γ}{3}\right)^{l_{}}$ 

Thus, if the prover can pass the testing phase with a probability greater than $\frac {+1}{p^{k}}+\left(1-\frac {d}{3}\right)^{l_{Q}}$ ,and the evaluation phase with a probability greater than $\left(1-\frac {2γ}{3}\right)^{l_{Q}}$ , the binding property of the scheme is ensured. ☐

## 4.1 Extractability

If the committed polynomial has $l$ variables,let $m=2^{l/2}$ ,so the matrix U being committed is an $m\times (m/k)$ matrix over GR $\left(p^{s}\right.$ $,$ kr)..Let $N=m/k$ .The keyto extraction lies in finding an invertible $NxN$ matrix over the Galois ring. In a finite field, a matrix formed by selecting $N$  linearly independent non-zero vectors of length Nis invertible. Randomly selecting Nvectors of length $N$  results in a non-negligible probability of the matrix being invertible. However, in a Galois ring, due to the presence of zero divisors, a set of linearly independent vectors does not necessarily form an invertible matrix.

We prove that a randomly chosen $NxN$ matrix over the Galois ring GR(p⁸, $,)$ is invertible with high probability. First, we consider the case where $p^{}>N$ and $1-\frac {N}{p^{}}$ is non-negligible.

**Lemma** **5.** Assuming that $1-\frac {N}{p^{r}}>0$ and is non-negligible,then by randomly selecting an $NxN$ matrix R from $GR\left(p^{s},r\right)$ ,there is a probability $of1-\frac {N}{p^{r}}$  that R is invertible.

Proof. Using the adjugate matrix method for matrix inversion,

$$R^{-1}=\frac {A^{*}}{\text {det}(A)}=\frac {1}{\text {det}(A)}\left[\begin{array}{ccc}R_{11}^{*}&\cdots &R_{1N}^{*}\\ :&\cdots &:\\ R_{N1}^{*}&\cdots &R_{NN}^{*}\end{array}\right]$$

Let $R_{ij}^{*}$ represent the algebraic cofactor of matrix R with respect to the(i,j) entry. We observe that if $\text {det}(R)$ is non-zero and not a zero divisor, then $^{-1}$ 

<!-- Transparent SNARKs over Galois Rings -->

<!-- 23 -->

exists. Now, we calculate the probability that $\text {det}(R)$  is non-zero and not a zero divisor.

$$\text {det}(R)=\sum _{σ\in S_{N}}\text {sgn}(σ)\prod _{i=1}^{N}A_{i,σ(i)}$$

where $S_{N}$ is the set of all permutations on $\{1,2,\cdots ,N\}$ ,and $\text {sgn}(σ)$  denotes the sign of permutation $σ$ . If $σ$  has an even number of inversions, then $\text {sgn}(σ)=1$ ;otherwise, $\text {sgn}(σ)=-1$ . In fact, $\text {det}(R)$  can be viewed as a multilinear polyno-mial in the $N^{2}$ variables $R_{ij}$ ,and the degree of the polynomial is at most N. Let us define this polynomial as $f_{R}$ .

By a known fact about Galois rings, all zero divisors in GR $\left(p^{}r\right)$ lie in the ideal $(p)$ .Therefore, we only need to show that $f_{R}$  is non-zero modulo $p$  with a non-negligible probability. Consider the isomorphism map $ψ$ 

<!-- $\begin{array}{c}\mathbb {Z}_{p^{s}}\\ c_{0}+c_{1}p+\cdots +c_{p-1}p^{s-1}\mapsto c_{0}\end{array}$ -->

For a polynomial $f$  of degree at most $d$  with coefficients in GR $\left(p^{s},r\right)$ ,we define a mapping $Ψ$ ,where $Ψ(f)$  is derived by applying $ψ$  to each coefficient of $f$ ,resuilting in a polynomial $f^{\prime }$ . Clearly, $Ψ$  is a homomorphism. If at some pointX, we have $(\mathbf {x})=0$ (mod p), then it follows that $Ψ()(ψ(\mathbf {x}))=0$ 

Let $Ψ\left(f_{R}\right)=f_{R}^{\prime }$ If $f_{R}^{\prime }$ evaluates to a non-zero value, then $f_{R}$ will not be a zero divisor in $\text {GR}\left(p^{s},r\right)$  . Since the variables $R_{ij}$  are randomly chosen from $\mathrm {GR}\left(p^{s},r\right)$  ,SO $ψ\left(R_{ij}\right)$ are randomly chosen from $\text {GF}(p,$ .By the Schwartz-Zippel lemma for fields, the degree of $f_{R}^{\prime }$ is at most $N,$ so the probability that a randomly chosen point evaluates to 0 is at most $\frac {N}{p^{r}}$ .Therefore,the probability that $f_{R}^{\prime }$ is non-zero is at least $1-\frac {N}{p^{r}}$ ,which is non-negligible. This completes the proof. ☐

If the ring GR $\left(p^{s},r\right)$ does not satisfy $p^{r}>N$ ,then the GR(ps,r)code used in encoding must be a $[l,n,d]-k-\text {GR}\left(p^{},r\right)$ code,where $p^{kr}»N.$  Choose a $k^{\prime }$ such that $k^{\prime }$  is a divisor of $k$  and $p^{k^{\prime }r}>N$ . At this point, the extractor can send $k^{\prime }$  challenges from $\text {GR}\left(p^{s},r\right)$ to simulate a challenge from GR $\left(p^{s},k^{\prime }r\right)$ .Then, using Lemma 8, we can obtain an invertible $NxN$ matrix with non-negligible probability.

If the probability that the matrix is invertible is $p_{i}$ ,by repeating the process $\text {poly}\left(\frac {1}{p_{i}}\right)$  times, we can ensure that with probability $1-\text {negl}(λ)$ , the matrix U can be successfully extracted.

## 4.2 More Efficient

When using polynomial commitment schemes in SNARKs, it is typically required that the challenge space for the verifier is exponential in size relative to the security parameter $λ$ , while the polynomial coefficients may come from a smaller Galois ring. Suppose the coefficients come from GR $\left(p^{},r\right)$ ,while the challenges

<!-- 24 -->

<!-- Yuanju Wei, Xinxuan Zhang, and Yi Deng -->

come from $\mathrm {GR}\left(p^{s},kr\right)$ .. In this case, when committing to the coefficients,each coefficient element needs to be treated as an element in GR $\mathcal {R}\left(p^{},\right.$ kr).This increases the cost of encoding and hashing by a factor of $k$ .

In Binius[17],, a two-dimensional expansion was uised to address this issue. In this work, we explain the problem from the perspective of “repetition”: using the observations that even though elements are treated as being in GR $\left(p^{},kr\right)$ during the encoding phase, the commitment still supports linear combinations over GR( $\left(p^{s},\right)$ . This allows the algebraic structures used during the commitment and evaluation phases to differ. Below, we present a polynomial commitment scheme where the coefficients come from GR $\left(p^{},r\right)$ ,but the challenges come from $\text {GR}\left(p^{s},kr\right)$ 

The polynomial commitment scheme is designed to compute a multilin-ear polynomial $f\left(x_{1},\cdots ,x_{l}\right)$ over GR $\left(p^{},\right)$ ,where the coefficients come from GR $\left(p^{s},r\right)$ and each $x_{i}\in \text {GR}\left(p^{s},kr\right)$ .Assume that the encoding over $\mathrm {GR}\left(p^{s},r\right)$ uses a k-[l,n,d]code,where $k\geq 1.$ 

## More Efficient Polynomial Commitment for Small Rings

## - Commit Phase

·The prover splits the multilinear polynomial with l variables into a ma-trix U of size $2^{/2}\times 2^{/2}$ $:$ $.L$ $m=2^{/2}$ . Each row of the matrix is encoded using a k-[tm,m,d] code to obtain a matrix $\hat {U}$ with $m$ rows, $\hat {U}_{1},\cdots ,\hat {U}_{m},$ where $\hat {U}_{i}\in \text {GR}\left(p^{s},kr\right)^{tm/k}$ ,and $\hat {U}_{i}[j$ represents the element in the i-th row and $j-th$ column of the matrix U. The prover constructs a Merkle Tree to commit to l $\hat {U}$ 

## -Testing Phase

**·Verifier→Prover**: A random matrix $R\in \left(\text {GR}\left(p^{s},r\right)\right)^{kxm}$ ,where each element of R is randomly chosen from $\text {G}\left(p^{},r\right)$ 

**·Prover** $\rightarrow$ Verifier: Treat every $k$  adjacent elements in each row of matrix $U$  as an element in GR $\left(p^{s},r\right)$ ,and compute $V=RU\in \mathrm {GR}\left(p^{},\right.$ $kr)^{k\times (m/k)}$ and calims that $V=RU$ . Then,send V to the verifier.

**·Verifier**: Choose a random set $Q$  of size $l_{Q}=Θ(λ)$ ,with $Q\subseteq [\mathrm {tm}/k]$ For each $j\in Q:$ 

- Verifier queries all $m$  entries of the corresponding “column” of $\hat {U}$ $\hat {U}_{1}[j],\cdots ,\hat {U}_{m}[j]$ . The verifier receives these values and the associated Merkle Tree paths from the prover. If one of the paths is invalid, reject.

-For each row $V_{i}$ of matrix $V,$  the verifier confirms that $\text {Enc}\left(V_{i}\right)=$ $\sum _{s=1}^{m}R_{i}[s]·\hat {U}_{s}[j]$ ,rejecting if the condition is not satisfied.

## - Evaluation Phase

·To compute $f\left(r_{1},\cdots ,r_{l}\right)$ ,calculate $\mathbf {q}_{\mathbf {1}}=\left(1-r_{1},r_{1}\right)\otimes \cdots \otimes \left(1-r_{l/2},r_{l/2}\right)\in$ $\text {GR}\left(p^{s},kr\right)^{m}$ and $\mathbf {q}_{\mathbf {2}}=\left(1-r_{l/2+1},r_{l/2+1}\right)\otimes \cdots \otimes \left(1-r_{l},r_{l}\right)\in \mathrm {GR}\left(p^{s},kr\right)^{m}.$ 

·Split each element of $\mathbf {q}_{1}$  into a $k$ -length vector over GR $\left(p^{s},r\right)$ .Let $\mathbf {q}_{1,i}$ represent the $i-th$ position of each element. Then,for each $\mathbf {q}_{1,i}$ ,perform the evaluation phase to get $k$  vectors $\mathbf {v}_{1},\cdots ,\mathbf {v}_{k}$ 

